{
  "phase": 11,
  "title": "Error Handling, Diagnostics, And Data Integrity",
  "diagnosticArtifacts": [
    {
      "operation": "import",
      "artifactPattern": "app-data/diagnostics/<modpack-slug>-<file-id>-assets.json",
      "status": "implemented",
      "userAction": "Open Settings, use the data folder action, then inspect the diagnostics directory."
    },
    {
      "operation": "validation",
      "artifact": "MCP validate_scheme structuredContent.diagnostic",
      "status": "implemented",
      "userAction": "Ask a connected MCP client to call validate_scheme for an open scheme."
    },
    {
      "operation": "export",
      "artifactPattern": "app-data/diagnostics/export-scheme-<scheme-id>-<format>.json",
      "status": "implemented",
      "userAction": "Export a scheme and inspect the diagnostics directory."
    },
    {
      "operation": "ai_tool_call",
      "artifact": "MCP tools/call structuredContent.diagnostic on rejected commands",
      "status": "implemented",
      "userAction": "Ask a connected MCP client to make an invalid out-of-bounds edit."
    }
  ],
  "dataIntegrityChecks": [
    "create_scheme uses one SQLite transaction for scheme, dimensions, and initial stage rows",
    "LibraryDatabase rejects schema_migrations versions newer than the supported app schema",
    "MCP invalid bulk mutations preserve the scheme block count and return a structured error"
  ]
}
